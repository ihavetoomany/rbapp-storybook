import { Button, type ButtonProps } from 'react-native-paper';

export type ResursButtonProps = ButtonProps;

export function ResursButton(props: ResursButtonProps) {
  return <Button {...props} />;
}
