// /savings-graph — SavingsGraphView (savings overview + area chart).

import React from 'react';

import { SavingsGraphView } from '@/src/features/activity';

export default function SavingsGraphRoute() {
  return <SavingsGraphView />;
}
