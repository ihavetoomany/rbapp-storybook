// /purchases — PurchasesListView (all purchases, newest first).

import React from 'react';

import { PurchasesListView } from '@/src/features/activity';

export default function PurchasesRoute() {
  return <PurchasesListView />;
}
